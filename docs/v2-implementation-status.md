# 家庭記帳 v2：1–7 開發狀態

> 日期：2026-08-13
> 環境：Firebase dev；所有金額與名稱均為示範資料

## 已完成

1. **分類與帳戶**：收入／支出分類、圖示、排序、封存；現金、銀行、信用卡、投資與外幣帳戶；初始餘額、淨資產、帳戶明細與帳務調整。
2. **正式交易**：收入、支出、轉帳、手續費、日期補記、備註、帳戶／分類／專案選擇；所有明細使用同一套整版 UI 編輯或作廢。
3. **預算與專案**：每月／年度分類預算及加總；專案設定、預算、收支與明細，並同時納入整體報表。
4. **定期項目**：週／月／年規則、預設確認、自動入帳、提前顯示、略過、編輯與封存；固定 occurrence ID 防止兩台裝置重複入帳。
5. **代墊與分帳**：我替別人代墊、別人替我代墊、純代墊、自己的份額、多人不同金額、個別剩餘與部分收還款；收還款不重複計入收支。
6. **查看與報表**：首頁真實摘要、預算明細、全部交易、收支行事曆、收入／支出／淨資產趨勢、支出分類與帳戶／專案明細。
7. **同步與防護**：Google 成員登入、Firestore 即時同步、Rules 家庭邊界、文件 revision 衝突檢查、軟刪除／封存，以及手機左緣右滑返回。

## 驗證證據

- 34 項自動測試通過：舊版後端安全 5、帳務核心 12、v2 衍生計算 8、Firebase Auth／Rules 9。
- v2 lint 與 production build 通過。
- Firebase Hosting 實際驗證：新增、編輯、作廢支出；帳戶餘額與報表回算；多人代墊部分收款與收款明細作廢；預算、專案、定期、行事曆；390×844 手機版與 1280×720 電腦版。

## 下一階段

已確認功能與 UI 對齊完成。下一步製作「舊資料匯入預覽」：先顯示筆數、日期範圍、帳戶／分類對應與新舊總額差異，經 Pei Ching 確認後才寫入；舊站、GAS、Sheets 與正式入口保持不動。

## UI 對齊進度

- 2026-08-11 第一批已完成首頁與記一筆：恢復原型的桌面比例與明細入口、首頁三項摘要／預算／交易層級、頂端通知與隱藏金額；記一筆改回獨立整版的返回＋四類型＋儲存頂欄。
- 支出／收入、轉帳、代墊、分類格狀選擇、帳戶清單選擇與專案清單選擇均沿用原本 Firestore 資料；編輯仍共用同一套頁面。
- 31 項帳務／Auth／Rules 測試、lint 與 production build 全數通過，並已部署 Firebase dev。1280×720 電腦版已實際截圖比對；390×844 手機版仍需實機畫面驗收，不在文件中假定通過。（此為當次歷史紀錄，最新驗證數見下方。）
- 2026-08-12 已完成全站 UI 對齊：帳戶、預算、專案、定期、代墊與分帳、分類、行事曆／清單、報表與各編輯頁均以已確認原型為準，不另增底部交易明細或代墊對象管理。
- 報表本月／近三個月／今年／自訂不跳通知；自訂帳戶與專案篩選只在「自訂」期間生效。定期自動入帳只在日期到達後執行，提前顯示不會提早入帳。
- 修正外幣收入、支出、手續費、代墊與帳務調整的台幣報表換算，避免有兩位小數的幣別因最小單位而放大 100 倍。
- 32 項測試、lint、production build 通過；Firebase dev 已重新部署。實際以 390×844 手機與 1280×720 電腦逐頁點擊驗收，瀏覽器無 error／warning。
- 詳細逐頁狀態見 `docs/v2-ui-parity-checklist.md`。下一步為一次性舊資料匯入預覽。

## 2026-08-12 記帳與代墊操作修正

- 首頁隱藏金額會同步遮蔽月摘要、預算金額／比例／進度條及交易金額。
- 未結清代墊只以實際剩餘金額判斷，並相容舊資料直接寫在份額內的結清金額；剩餘為 0 不再出現在首頁通知或未結清清單。
- 新增收支預設選取排序第一的分類與帳戶；分類預算餘額改到右側值的下一行，不擠壓分類名稱；金額輸入字級與其他欄位一致。
- 新增記帳底部固定為「儲存／再記一筆」；後者儲存後保留交易類型並重設新表單，同時重新帶入排序第一的預設分類與帳戶。
- 單人代墊只填一次姓名，應還金額由消費總額扣除自己的份額後自動計算；新增第二人後才顯示每人的個別金額欄。
- 餘額調整頁固定使用「帳務調整」分類，因此不再顯示重複的分類欄，儲存移至右上角。
- Firebase dev 已部署並完成 390×844 手機操作驗收；瀏覽器 console 無 error／warning。

## 2026-08-13 排序、報表與明細呈現修正

- 帳戶與分類管理保留拖曳排序，並補上「上移／下移」按鈕；滑鼠、觸控或瀏覽器拖曳支援不完整時仍可精準調整順序。
- 多人代墊的消費總額會由各人應還金額加上自己的份額即時加總；新增、修改、移除對象或切換是否包含自己的消費都會同步更新。
- 記帳整版頂端右側新增「儲存」，下方仍維持左「儲存」、右「再記一筆」，三者沿用同一套驗證與存檔邏輯。
- 統計報表的支出分類可點入完整頁面，依目前選擇的本月、近三個月、今年或自訂區間顯示該分類總額與交易明細。
- 所有共用交易明細改為按日期分組；日期只顯示在分組標題，個別明細不重複標日期，且每筆仍可點入共用記帳 UI 編輯。
- 32 項自動測試、lint 與 production build 通過；Firebase dev 已重新部署。手機與電腦版已驗證新增入口、分類明細、日期分組及排序控制；自動化環境禁止模擬實際滑鼠拖曳，因此拖曳以原生 `draggable`／觸控事件與可實際改序的上下移按鈕共同保障。
- 代墊方向精簡為「我先付／別人先付」；單人代墊移除可由欄位直接理解的自動計算說明，底層計算規則不變。原型來源與瀏覽預覽已同步更新。
- 「我先付／別人先付」兩種代墊方向的主金額欄統一稱為「消費總額」。
- 待確認定期項目保留一鍵確認；點項目內容會開啟完整「確認定期項目」頁，可修改本次日期、金額、分類、帳戶、專案與備註後入帳，且不改動下一期仍使用的規則預設值。
- 交易明細的行事曆／清單切換移至頂端 Bar 右側，以兩個線條圖示呈現；「交易明細」繼續置中，頁面內容不再重複顯示切換列。
- 上述調整通過 32 項測試、lint、production build 並部署 Firebase dev；390×844 手機版已驗證頂端切換與兩種明細模式，console 無錯誤。當下 dev 無待確認資料，因此沒有為畫面測試擅自改動示範定期規則。

## 2026-08-13 報表資訊架構重製

- 報表第一層改為「支出／收入／結餘」；支出與收入提供月、近 6 個月、年、自訂，結餘提供月、年、自訂。
- 月份與年度可前後切換；自訂保留日期、帳戶與專案篩選。本階段依 Pei Ching 決定不做幣別篩選。
- 支出／收入可在圓環圖與柱狀圖間切換，顯示總額、分類占比、分頁圖例與可排序分類明細；分類可進完整交易明細。
- 結餘改為收入／支出／結餘摘要、三線折線圖與可排序日期明細；日期或月份可進完整交易明細。
- 新增指定月份的月／近 6 個月／年度區間測試；34 項測試、lint、production build 全數通過並部署 Firebase dev。
- 視覺驗收缺口：隔離瀏覽器無家庭帳號登入狀態，且沒有可接管的 Chrome，因此本次尚未完成 390×844 與 1280×720 的登入後畫面／console 驗收；正式入口與真實資料未修改。
- 登入事故與修正：交付時誤貼 `web.app`，但 Google Auth 設定與原驗收入口是 `firebaseapp.com`；部分瀏覽器會因跨網域 redirect／舊首頁快取而失敗。現在 `web.app` 點登入會先轉到 `firebaseapp.com`，Hosting 全站改為 `no-cache`，並以實際瀏覽器點擊與 HTTP 標頭完成驗證。

## 2026-08-13 首頁、趨勢與代墊明細調整

- 報表頂端直接顯示「支出／收入／結餘」，移除重複的「統計報表」標題；幣別篩選仍依決定暫不製作。
- 首頁移除預算區塊，月摘要下方直接顯示最近 10 筆交易；預算功能保留在「更多管理」。
- 所有日期分組統一顯示為「8 月 12 日（週三）」並禁止拆行，修正日期與金額在手機版錯位。
- 結餘頁改為收入、支出、本期結餘、期末淨資產四項摘要；趨勢圖以收入／支出柱狀圖搭配結餘／淨資產折線，明細同步列出四項數字。
- 代墊原始交易改標示「我先代墊・對象」或「某人先代墊」；收回與歸還分別標明「不計收入」與「不計支出」，金額依收／還方向顯示，不再把收回誤畫成收入或把還款顯示為正數。
- 完整 34 項測試、lint、production build 通過並部署 Firebase dev；已用登入後資料完成 390×844 手機版與 1280×800 電腦版畫面驗收。

## 2026-08-13 首頁交易中心與四項報表

- 首頁內容改為完整交易中心，頂端中央以日曆／清單圖示取代「首頁」文字；右側待確認定期、未結清代墊與隱藏金額維持原位置。
- 日曆與清單共用年月選擇列，可用前後按鈕或月份選擇器直接切換；清單只列目前月份，年月列位於全部／支出／收入／轉帳上方。
- 報表拆成支出、收入、結餘、淨資產四項。結餘不再含淨資產，只保留月／年：月為截至選定月份的最近 6 個月，年為截至選定年份的最近 6 年。
- 結餘圖以左軸顯示收入／支出柱狀金額、右軸顯示每期「收入－支出」折線；六個期間即使沒有交易仍保留零值。
- 淨資產獨立顯示期初、期末與區間增減，並依帳戶開立日期計算，避免把 2026 年建立的初始餘額倒灌到更早年度。
- 34 項自動測試、lint、production build 通過並部署 Firebase dev；390×844 手機與 1280×800 電腦版已實際操作驗收。
- 清單模式保留目前月份的支出、收入、結餘摘要；非本月時會以所選月份命名，避免把歷史月份誤標成本月。
- 首頁頂端左側新增「今天」，會回到本月並在日曆重新選取今天；日曆另以底線標記今日，選取方塊與日曆外框增加內距，靠邊日期不再貼框。
